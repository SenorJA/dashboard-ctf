services:
  - type: web
    name: vulnforge-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn backend.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --log-level info
    envVars:
      - key: PORT
        value: "8000"
