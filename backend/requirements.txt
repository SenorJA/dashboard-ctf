fastapi
uvicorn
gunicorn
websockets
paramiko
Pillow>=10.0.0
supabase
reportlab
python-dotenv
python-multipart
psycopg2-binary  # optional — for automatic DB bootstrap (set SUPABASE_DB_PASSWORD)
httpx            # optional — for kali-mcp integration (set KALI_MCP_URL)
watchdog         # optional — native fs events for the plugin hot-reload watcher (falls back to polling if absent)
